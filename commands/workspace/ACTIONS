code search
    index files
    search files

    interact with git, separate index for each branch

    providers
      codesearch
      bleve
      elasticsearch

    codesearch wrapper
      ~/.codesearch/indexes/<index>/<branch>
      only works if that current file is checked out

    elasticsearch
      requires instance to be running, easy enough
      requires means to index files
        index mapping

      logstash plugin

cli
    captures output okay
    after screenshot does not appear to be "after" cmd completes
    does not commit

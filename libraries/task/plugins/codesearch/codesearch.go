package codesearch

type Codesearch struct {
	// if specified, store index separate from master index
	GlobalIndexName string
}

// this can be abstracted away
/*
func Initialize(taskName string) {
	// read configuration file
}
*/

func Load(config interface{}, prefix string) {

}
